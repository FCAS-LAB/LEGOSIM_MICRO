# Phase 1 configuration.
phase1:
  # Process 0
  - cmd: "$BENCHMARK_ROOT/bin/decoder_cu"
    args: ["1", "0"]
    log: "gpgpusim.1.0.log"
    is_to_stdout: false
    clock_rate: 1.1
    pre_copy: "$SIMULATOR_ROOT/gpgpu-sim/configs/tested-cfgs/SM7_QV100/*"
  # Process 1
  - cmd: "$SIMULATOR_ROOT/snipersim/run-sniper"
    args: ["-c", "Intel.cfg", "--", "$BENCHMARK_ROOT/bin/decoder_c", "0", "0"]
    log: "sniper.0.0.log"
    is_to_stdout: false
    clock_rate: 2.5
# Phase 2 configuration.
phase2:
  # Process 0
  - cmd: "$SIMULATOR_ROOT/popnet_chiplet/build/popnet"
    args: ["-A", "2", "-c", "1", "-V", "4", "-B", "12", "-O", "12", "-F", "3", "-L", "1000", "-T", "1000000000000", "-r", "1", "-I", "../bench.txt", "-G", "/home/qc/Chiplet_Heterogeneous_newVersion_gem5/Chiplet_Heterogeneous_newVersion/popnet_chiplet/test/mesh_2_2.gv", "-R", "4", "-D", "../delayInfo.txt", "-P"]
    log: "popnet.log"
    is_to_stdout: false
    clock_rate: 1

# File configuration. (Not used yet)
bench_file: "./bench.txt"
delayinfo_file: "./delayInfo.txt"
